# Deletion Report — 2026-06-19

## Scope
Conservative repository surface-area reduction. Two reversible action classes:
(1) stop tracking large regenerable build artifacts, and (2) relocate
root-level observational/assessment documents into the existing archive index.
No deletion of source, tests, migrations, schemas, or enforcement logic.

## Untracked regenerable artifacts (kept on disk, added to `.gitignore`)
- `worker-configuration.d.ts` (~492K) — generated by Wrangler. Regenerate with
  `npx wrangler types`. Required locally by `tsconfig.json` (`types`/`include`).
  (No dedicated npm script is added: package scripts are governance-classified
  surfaces, so a one-off local typegen command is invoked directly.)
- `graph/topology-viewer.html` (~476K) — generated by `npm run graph:view`.
- `graph/runtime-topology.sample.json` (~592K) — generated by
  `npm run graph:runtime`; already matched by the existing `graph/*.json`
  ignore rule.

## Relocated observational documents (root → `artifacts/inventory/`)
Moved with `git mv` (history preserved), lineage front-matter added, indexed in
`artifacts/inventory/INDEX.md`. Each verified to have no runtime, test,
workflow, or script reader; the only remaining mentions are bare-filename prose
in `artifacts/closure/phase-matrices/PHASE3_CLOSURE_MATRIX.md` (archived
evidence).
- `ARTIFACT_INVENTORY.md`
- `INVENTORY_SOURCE_MAP.md`
- `DRIFT_ANALYSIS.md`
- `LIGHTWEIGHT_CLOSURE_RECOMMENDATIONS.md`
- `REPOSITORY_REDUCTION_ASSESSMENT.md`
- `LIGHTWEIGHT_REDUCTION_ASSESSMENT.md`
- `AGENT_BYPASS_INVENTORY.json`
- `PHASE3_EXECUTION_SURFACE_INVENTORY.json`

## Safety constraints preserved
- No changes to `src/**` execution pipeline or runtime handlers.
- No schema edits under `schemas/**` or `runtime/legitimacy/schemas/**`.
- No test logic mutation under `tests/**`.
- No edits to `governance/**` enforcement registries.
- Legacy throw-stub entry points (`worker.js`, `server.js`, `gateway.js`,
  `registry.js`, `compile-decision.js`) retained — they are intentional
  fail-closed tripwires protected by `PRESERVATION_MANIFEST.md`.
- Root `BYPASS_PATHS.json` and `EXECUTION_SURFACES.json` retained — read
  directly by tests and runtime code (10 references each).

## Verification performed
- `npx wrangler types` regenerates `worker-configuration.d.ts`.
- `git check-ignore` confirms all three artifacts are now ignored and remain on
  disk.
- `npm run test:smoke` passes (112/112).
- `npm test` shows no new failures attributable to this change; the 16
  pre-existing failures (`tests/registry-lineage-migrations.test.mjs` family)
  reproduce identically on clean `origin/main` and are environmental
  (local SQLite/D1 tooling unavailable).
