{
  "name": "mindshift-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "echo 'NULL — production deploys must use the MindShift governed deploy boundary' && exit 1",
    "dev": "wrangler dev --local",
    "d1:migrate:local": "wrangler d1 migrations apply mindshift-demo-local --local"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
